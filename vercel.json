{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "rewrites": [
    { "source": "/issues/:path*", "destination": "/issues/:path*" },
    { "source": "/((?!issues|assets|data).*)", "destination": "/index.html" }
  ]
}
